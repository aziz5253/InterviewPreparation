logging:
  level:
    org.springframework.cloud.config: DEBUG

server:
  port: 8888

spring:
  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/config-repo/
